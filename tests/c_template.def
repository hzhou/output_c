page: t, from template/main.c
    module: c

subcode: custom
    $in_function main
        $print Hello from custom code
        $global f_x
        f_x = 3.14159
        $print x = $f_x

        tf_x = f_x * 2
        $print tf_x = x * 2 = $tf_x

subcode: _autoload
    $list F
fncode: F(n)
    return n*n

subcode: fn_A
    n_scale = 2
    x=3
    return x*n_scale

